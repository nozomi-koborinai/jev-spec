diff --git a/src/context/glob-matcher.ts b/src/context/glob-matcher.ts
index 6e6fb64..57d68c4 100644
--- a/src/context/glob-matcher.ts
+++ b/src/context/glob-matcher.ts
@@ -15,7 +15,7 @@ export async function resolveGlobPatterns(
   cwd: string = process.cwd()
 ): Promise<string[]> {
   const include: string[] = [];
-  const ignore: string[] = [...DEFAULT_SENSITIVE_IGNORE_PATTERNS];
+  const ignore: string[] = [];
 
   for (const pattern of patterns) {
     if (pattern.startsWith('!')) {
