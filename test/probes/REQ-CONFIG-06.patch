diff --git a/src/runner/engine.ts b/src/runner/engine.ts
index c000411..645abc9 100644
--- a/src/runner/engine.ts
+++ b/src/runner/engine.ts
@@ -95,8 +95,6 @@ export async function runChecks(
   config: JevSpecConfig,
   options: RunOptions = {}
 ): Promise<OverallCheckResult> {
-  validateConfig(config);
-
   const cwd = options.cwd ?? process.cwd();
   const startTime = Date.now();
 
