diff --git a/src/cli/commands/check.ts b/src/cli/commands/check.ts
index 4373d14..4382676 100644
--- a/src/cli/commands/check.ts
+++ b/src/cli/commands/check.ts
@@ -76,7 +76,7 @@ export async function checkCommand(options: CheckCliOptions = {}): Promise<numbe
       console.log(outputText);
     }
 
-    return result.passed ? 0 : 1;
+    return result.passed ? 1 : 1;
   } catch (error: unknown) {
     const message = error instanceof Error ? error.message : String(error);
     console.error(`\n[jev-spec error] ${message}`);
