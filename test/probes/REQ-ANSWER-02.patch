diff --git a/src/runner/engine.ts b/src/runner/engine.ts
index c000411..566ffdc 100644
--- a/src/runner/engine.ts
+++ b/src/runner/engine.ts
@@ -187,7 +187,6 @@ export async function runChecks(
       const typedRubric = rubric as AnyRubric;
       const result = rubricResults[name];
       if (!result) {
-        targetPassed = false;
         evaluations.push({
           rubricName: name,
           rubric: typedRubric,
